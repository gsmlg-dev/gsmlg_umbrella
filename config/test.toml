# Test environment configuration
# Overrides base.toml values for testing

[logger]
log_level = "warning"

[database]
database = "gsmlg_test"
pool_size = 5

[web]
url = "http://localhost:4112"
port = 4112
secret_key_base = "test_secret_key_base_for_testing_only_at_least_64_chars_long_here"

[admin_web]
url = "http://localhost:4113"
port = 4113
secret_key_base = "test_secret_key_base_for_testing_only_at_least_64_chars_long_here"

[commander]
start = false
